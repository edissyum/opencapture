Ajout de métadonnée : 

Le nom du champ doit être de type 'custom-birth_date'. Il faut bien laisser le custom-: 

	[custom-birth_date]

Récupérer les positions XY du champ à capturer (via un logiciel tier, GIMP par exemple). Il faut capturer un point en haut à gauche du champ et un autre en bas à droite (pour faire un rectangle)
Dans le fichier TYPOLOGIE.ini remplir le champs positions comme ça : 

	position    = ((2110, 185), (2395, 265))

Les deux premieres positions sont les coordonnées en haut à gauche. Les deuxième sont celles en bas à droite.
Si nécessaire, mettre une regex :
	regex       = dateRegex

La REGEX doit avoir le même nom que dans le fichier de regex fr_FR.json

Il faut ensuite choisir le type de la métadonnées entre string, number ou date. Un traitement différent sera appliqué
	type        = date

Ensuite, il faut mentionner le nom de la colonne en BDD. Il faudra bien penser à créer cette nouvelle colonne dans la BDD 'invoices' ainsi qu'une deuxième colonne ayant le même nom plus '_position'
	column      = birth_date

Dans ce cas là, il faudra créer une colonne 'birth_date' ainsi qu'une colonne 'birth_date_position'. Le type de colonne pour la position est VARCHAR

Finalement, il faut choisir dans quelle partie de l'image la recherche de la métadonnées se fera. header, footer ou full
	target      = header 


Pour ajouter les champs dans le formulaire : 

	Créer un nouvel onglet si nécessaire. Vous pouvez vous baser sur l'onglet "Patient" déjà crééer. 
	Pour l'ajout dans le fichier XML, les <input> doivent avoir un "name" formaté comme suivant :

		name="customInfo_birth_date"

	Il faut garder le 'customInfo_' afin que le XML soit bien construit

	Il faut également bien penser à modifier les valeurs appelé dans les champs x1_original, y1_original, x2_original, y2_original et value