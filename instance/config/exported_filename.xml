<?xml version="1.0" encoding="UTF-8" ?>
<ROOT>
    <NAME>
        <ELEMENT>
            <type>column</type>  <!-- 'column' for database data (column name) || 'text' for brut text || 'date' with year/month/day/full_date as filter -->
            <value>name</value> <!-- column name or brut text -->
            <table>suppliers</table> <!-- suppliers or invoices -->
            <filter/>
        </ELEMENT>
        <ELEMENT>
            <type>column</type> <!-- 'column' for database data (column name) || 'text' for brut text || 'date' with year/month/day/full_date as filter -->
            <value>invoice_number</value> <!-- column name or brut text -->
            <table>invoices</table> <!-- suppliers or invoices -->
            <filter/>
        </ELEMENT>
        <ELEMENT>
            <type>text</type> <!-- 'column' for database data (column name) || 'text' for brut text || 'date' with year/month/day/full_date as filter -->
            <value>F</value> <!-- column name or brut text -->
            <filter/>
        </ELEMENT>
        <ELEMENT>
            <type>column</type> <!-- 'column' for database data (column name) || 'text' for brut text || 'date' with year/month/day/full_date as filter -->
            <value>register_date</value> <!-- column name or brut text -->
            <table>invoices</table> <!-- suppliers or invoices -->
            <filter>full_date</filter>
        </ELEMENT>
    </NAME>
    <!-- Separator, used to separate all the elements between them -->
    <SEPARATOR>
        <value>_</value>
    </SEPARATOR>
</ROOT>