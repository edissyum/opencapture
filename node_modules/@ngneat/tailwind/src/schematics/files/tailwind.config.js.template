module.exports = (isProd) => ({
    prefix: '',
    purge: {
      enabled: isProd,
      content: [<% if (appsDir) {%>
        './<%= appsDir %>/**/*.{html,ts}',<% } else {%>
        '**/*.{html,ts}',<% } %><% if (libsDir) {%>
        './<%= libsDir %>/**/*.{html,ts}',<% } %>
      ]
    },
    darkMode: false, // or 'media' or 'class'
    theme: {
      extend: {},
    },
    variants: {
      extend: {},
    },
    plugins: [],
});
