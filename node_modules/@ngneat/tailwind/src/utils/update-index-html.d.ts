import type { workspaces } from '@angular-devkit/core';
import type { Rule } from '@angular-devkit/schematics';
import { TailwindDarkMode } from '../schematics/schema';
export declare function updateIndexHtml(projectName: string, darkMode: TailwindDarkMode, updateWorkspaceFn: (updater: (workspace: workspaces.WorkspaceDefinition) => void | PromiseLike<void>) => Rule): Rule;
